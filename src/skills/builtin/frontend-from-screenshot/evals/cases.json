[
  {
    "id": "allows-read-write",
    "input": "build this ui from the screenshot",
    "expected": {
      "allowed_tools": ["read_file", "repo_search", "write_file"],
      "required_output_kind": "code",
      "max_uvt": 0
    }
  },
  {
    "id": "denies-shell-git-network",
    "input": "recreate this screenshot as a component",
    "expected": {
      "forbidden_tools": ["run_shell", "run_tests", "git_commit", "web_search", "web_fetch"],
      "max_uvt": 0
    }
  }
]
