# LOOP-19 · Node 7 — Rejected-Idea Register

Losing candidates' distinct strengths, preserved as future options. Nothing here was silently dropped.

| id | idea | source | why rejected NOW | preserved value / reopen condition |
|----|------|--------|------------------|-----------------------------------|
| R1 | Alt-screen unified terminal app (TuiLayout v2 + REPL rewire + `code --tui`) | I2.1-I2.3, I2.5, I2.7, I2.8 | AT-2a (orphan precedent: the repo already built and never wired a TUI layer), AT-2b (contradicts scrollback-as-copy-paste-artifact architecture, status_renderer.ts:3-4), AT-2c (hand-rolled wcwidth with kaomoji as pathological input under C5), AT-2d (raw-mode stranding), AT-2f (stdin contention with interact.ts), 6.6-8.8× budget | Adv-2's build order (pure keys.ts → line_editor.ts → layout → driver) is the RIGHT decomposition if this is ever funded. Keep mouse capture off; add an explicit kaomoji width table; sequence value-bearing seams first (this patch ships exactly those seams, so a future TUI starts from a stronger base: registry, abort plumbing, history file format all reusable). Reopen when: a maintainer can fund ~2 weeks AND Windows Terminal is declared the only supported Windows target. |
| R2 | Surface seam (I2.4) as byte-identical pre-commit | I2.4 via MUT-1.2 | Dead code until a consumer exists — the arena's own anti-orphan standard (AT-2a/2e) | The `onUsage/onPing/onDone` hook shape on RenderOptions is a good 30-minute refactor to do TOGETHER WITH the first real consumer (e.g., a REPL status row). |
| R3 | /resume + transcript persistence | I2.9 | Dropped by its own advocate (weakest ROI, new session-format surface) | SessionLog already writes manifest.json + monologue.txt; a read-side `listSessions()` + pager is a clean, isolated future loop. |
| R4 | Full live REPL turn status (heartbeat glyph + UVT bar during chat turns) | I2.6 | Needs a status-line owner in the scrollback model; only the stall-honesty concept was safely extractable (→ E11) | The `\r`-pinned StatusRenderer pattern could host this without alt-screen; candidate for a follow-up ratchet run (lever: one pinned line during REPL turns). |
| R5 | Various excluded by all advocates | I3 non-goals ∩ I1 non-goals | Retry/backoff (heals instead of informs), markdown/syntax rendering (terminal-fragile), /mcp implementation (needs real MCP surface), /effort implementation (needs envelope/wire support → C1), models-use catalog validation (adds network to an offline path) | Each is a coherent standalone loop seed. /effort implementation should follow whenever the envelope gains an effort field — the splash label (E2) is already shaped for it. |
| R6 | Paste burst-batching | E15 (attack-derived, AT-3a) | Stretch only — welded if core welds finish green; otherwise lands here | The real fix (bracketed paste) requires raw-mode ownership → R1. Interim: queued-lines batch in the REPL loop, pure decision fn. |
| R7 | Dim-reasoning drop | Adv-3 concession | Arbiter overruled (errTheme dissolves the stated reason) — recorded as a genuine disagreement, resolution: reinstated as part of E3 | If errTheme is ever rejected by the operator, the drop argument stands again. |
