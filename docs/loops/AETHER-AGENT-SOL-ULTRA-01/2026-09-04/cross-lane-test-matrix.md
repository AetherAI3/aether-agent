# Cross-Lane Test Matrix

Focused counts overlap and must not be summed into a full-suite count.

| Contract crossing | Permanent surface | Captured result | Boundary |
|---|---|---|---|
| Chat/code transport → typed lifecycle → human/JSON outcome | chat, code, turn lifecycle, verify gate | integrated set included in 88/88 | real paid/deployed turn unproven |
| Cosmetic progress → bounded timeout → cleanup | chat, code, event source | alternating-frame, parked-iterator, hung-tool regressions pass | real network timing unproven |
| Voice → capability → promo → transport | Voice/capability/promo/transport | Voice suite 52/52; broader set 67/67 | physical devices unproven |
| Voice → embedded terminal disposal | voice-session/terminal-session | 100 cycles; zero ephemeral resources | fake ports only |
| Settings → scoped stores/adapters/view | settings suites | 75 pass, 0 fail, 1 Windows file-link skip | external Online/Actions ports absent |
| Settings reset → exact plan → compensation | settings command/registry/store | 18/18 command tests; preview byte-identical | trusted single-adapter contract retained |
| Terminal remount → replay/gap → one outcome | terminal/TUI | 48/48 focused; 47/47 five repeats | browser/Electron host unproven |
| Cross-lane lifecycle churn | terminal/Voice/settings/MCP stress | 100 outcomes; max listeners 1; zero measured deltas | deterministic fakes |
| Cloud Voice contract → Agent mirror | `voice_contract_fixture.test.ts` | pinned to `f91d677...`; hash verified | Cloud PR not merged |
| Cloud CI schema → safe Agent editor | CI contract/parser/adapter tests | closed, bounded JSON-subset editor; 14 focused CI tests in full run | no Actions runner authority |
| Cloud PR #1483 | hosted checks | required jobs green at `ee60ab4` | broad jobs are merge-candidate; exact-head proof separate |

## Required final Agent gates

`npm ci --ignore-scripts`, `npm run typecheck`, `npm test`, `npm run docs:check`, `npm run verify:production`, `npm run release:truth`, `npm audit --audit-level=high`, and `npm pack --dry-run --ignore-scripts`, followed by hosted Ubuntu, Windows, supply-chain, and CodeQL checks.

## Real-host gaps

PTY interaction, browser/Electron remount, physical audio, live Ollama, deployed zero-UVT, live MCP OAuth, Online entitlement, and Actions execution remain `UNPROVEN`.
