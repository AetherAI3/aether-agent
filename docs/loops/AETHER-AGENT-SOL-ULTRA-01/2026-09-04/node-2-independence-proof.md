# Node 2 — Independence Proof

| Pair | Shared mutable leaf | Dependency | Result |
|---|---|---|---|
| Lifecycle / Voice | none | Voice injects the existing send bridge | independent |
| Lifecycle / Terminal | none before root wiring | shared outcome vocabulary | independent after reducer freeze |
| Terminal / Settings | none | shared sink/width facts only | independent |
| Registry / Store | separate modules | store implements adapter contract | ordered |
| Settings / CI adapter | separate leaf | CI adapter registers descriptors | ordered |
| Agent / Cloud | separate repositories | Agent pins immutable Cloud commits/hashes | repository-isolated |
| Leaves / Root | registries/docs/workflows only at integration | root wires after focused green | deliberately sequenced |

Subagents created no commits; root owns the final semantic history. Actual uncommitted diff review found no unexpected package-version, lockfile, database, deployment, or production configuration mutation. Shared checkout use remains a disclosed isolation deviation, so hosted exact-head checks and reviewer inspection—not lease intent—are authoritative.
